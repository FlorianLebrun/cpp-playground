set(target SegmentAllocTable)

add_subdirectory(mimalloc)

append_group_sources(files FILTER "*.c|*.cpp|*.h|*.hpp" DIRECTORIES "./test" "./src" "./src/common" "./src/win32")

add_executable(${target} WIN32 ${files})

target_link_libraries(${target} PRIVATE mimalloc-static)
target_link_options(${target} PRIVATE /SUBSYSTEM:CONSOLE)


